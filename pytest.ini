[pytest]
testpaths = tests
# Windows: avoid pytest-current junction cleanup (WinError 5 / Access Denied)
# after an otherwise green run — see pytest-dev/pytest#7491 and Win11 reparse issues.
tmp_path_retention_count = 0
tmp_path_retention_policy = none
addopts = --basetemp=.pytest_tmp
