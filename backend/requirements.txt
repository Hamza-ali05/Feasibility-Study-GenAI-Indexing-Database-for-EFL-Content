# EFL IndexDB — backend dependencies (final)
# Feasibility Study: GenAI Indexing Database for EFL Content
# Consolidated from Prompt 0-E (pipeline + API + live features)

# ── Pipeline + API ──────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.29
websockets>=12.0
pydantic>=2.7
sentence-transformers>=2.7
faiss-cpu>=1.8
numpy>=1.26
pandas>=2.2
pyarrow>=16
scikit-learn>=1.5
imbalanced-learn>=0.12
shap>=0.45
lime>=0.2
matplotlib>=3.9
seaborn>=0.13
pdfplumber>=0.11
beautifulsoup4>=4.12
lxml>=5.2
chardet>=5.2
tqdm>=4.66
tabulate>=0.9
joblib>=1.4
python-dotenv>=1.0
httpx>=0.27
scipy>=1.11

# ── Live / real-time features ───────────────────────────────────────────
python-multipart>=0.0.9
anthropic>=0.34
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
bcrypt>=4.0
apscheduler>=3.10
sse-starlette>=2.1

# ── Dev / test / security audit ─────────────────────────────────────────
pytest>=8.0
pip-audit>=2.7
